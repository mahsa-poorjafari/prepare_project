.form-group.upload-pic.well      
  = picture.file_field :image, :class => "hide"
  = image_tag (picture.object.image.present? ? picture.object.image(:medium) : "missing.png"), :class => "enable_file_select", :style => "height:50px; width:50px" 

  .form-group
    توضیحات
    = picture.text_area :description, :rows => 2, :class => "form-control"
  
    = picture.link_to_remove image_tag ("Delete_Icon.png"), :class => "remove-task", :style => "height:30px; width:40px" 
.clrear-float
