= form_for @page do |f|
  - if @page.errors.any?
    #error_explanation
      %ul
        - @page.errors.each do |attr, msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :html_text
    = f.text_area :html_text
  .actions
    = f.submit 'Save'
