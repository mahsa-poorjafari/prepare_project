
-if is_admin?
  = link_to 'تعریف محصول جدید', new_product_path, :class => "btn btn-default"
.products
  - @products.each do |product|      
    .pro_wrapper{align: "center", :id => I18n.locale}                
      = link_to product do
        %p.pro-info  
          = image_tag product.pictures.first.image(:small)
          .line
          %p= product.title 
        
    

 





